# Handoff 2026-05-13

## 今日完成

### grade_1~10 徽章图（已提交）
- unlocked 图：10张，已去白底（RGBA 透明通道）→ `src/kid_app/static/badges/grade_N-u.png`
- locked 图：统一用 grade_5-l.png（用户决定）+ DB 已更新
- DB `achievement_badges`：v2（is_current=1）全部指向正确 URL

### 成就卡片 v3 前端样式（已提交）
文件：`src/kid_app/static/badge-design-v3.html`

**最终确认的设计：**
1. **布局**：`justify-content` 默认 + info `flex:1` + `max-width:280px` + `padding-left:40px` — 避免右侧留白过多
2. **已解锁**：左上角金色光晕（`::before` radial-gradient）— 无顶部渐变条
3. **未解锁**：整体 `saturate(0)` 灰化 + 锁图标；hover 时恢复原色（`saturate(1) brightness(0.75)`）
4. **白底去除**：PIL 处理 RGBA 透明通道（threshold=200）
5. **动效**：全保留（入场/hover badge弹跳/click ripple+挤压）
6. **Grade 数字叠加圆圈**：已去掉（badge 图本身已含一级标识）
7. **Type pill**：按类型自动变色（突破=橙/段位=紫/巅峰=蓝/执着=土黄/神秘=青）

**关键 CSS（badge-img）：**
```css
.badge-img {
  width: 120px; height: 120px;
  object-fit: contain;
  filter: drop-shadow(0 4px 10px rgba(180,140,20,0.4));
}
.card-v3.locked .badge-img {
  filter: saturate(0) brightness(0.8) drop-shadow(0 2px 6px rgba(0,0,0,0.15));
}
.card-v3.locked:hover .badge-img {
  filter: saturate(1) brightness(0.75) drop-shadow(0 2px 8px rgba(0,0,0,0.2));
}
```

### migrate_achievements.py（已更新）
- LOCKED_TPL 已更新为生铁轮廓风格
- 包含 grade_1~10 完整定义，可重复运行

## 待做

### 合并 v3 样式到生产模板
下一步要把 `badge-design-v3.html` 的 CSS + JS 合并到 `src/kid_app/templates/achievements.html`，包括：
- `.card-v3` 样式替换原有 `.ac-card` 内卡样式
- GSAP 动效 JS 合并
- `.card-v3.locked .badge-img` 等新选择器
- info 区 `max-width` / `padding-left` 调整
- 去掉旧 `.milestone-badge-wrap` 相关样式

### DB 数据（已完整）
- `data/dizi.db`：achievements 28条 / achievement_stats 28条 / achievement_badges 52条
- grade_1~10 unlocked 图：已去白底，透明背景
- grade_5-l.png：统一 locked 图

## 已推送 commit
- `0758b76` feat: 成就卡片v3设计 - 布局优化/去掉白底/灰化方案/动效保留
